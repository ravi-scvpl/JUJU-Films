import React from 'react';
import NotFound from '@/views/NotFound';

export const metadata = {
  title: 'Page Not Found | JUJU Films',
  robots: 'noindex, nofollow',
};

export default function NotFoundRoute() {
  return <NotFound />;
}
