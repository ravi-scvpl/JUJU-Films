import React from 'react';
import Login from '@/views/admin/Login';

export const metadata = {
  title: 'Admin Login | JUJU Films',
  robots: 'noindex, nofollow',
};

export default function AdminLoginRoute() {
  return <Login />;
}
